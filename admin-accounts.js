(async()=>{let sb;let filter='PENDING';const list=document.getElementById('list');sb=initSupabase();try{await requireAdmin();}catch(e){list.innerHTML='<div class="empty">Admin access required. <a href="/admin.html">Go to admin login</a></div>';return}
document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));b.classList.add('active');load()});
async function load(){let q=sb.from('profiles').select('id,email,is_admin,account_status,email_verified_at,created_at,approved_at,rejected_at,full_name,contact_number,whatsapp_name,address').eq('is_admin',false).order('created_at',{ascending:false});if(filter!=='ALL')q=filter==='PENDING'?q.in('account_status',['PENDING_EMAIL','EMAIL_VERIFIED_PENDING_ADMIN']):q.eq('account_status',filter);const {data,error}=await q;if(error){list.innerHTML='<div class="empty">'+error.message+'</div>';return}if(!data?.length){list.innerHTML='<div class="empty">No accounts in this section.</div>';return}list.innerHTML=data.map(u=>{const verified=!!u.email_verified_at;const status=u.account_status||'PENDING_EMAIL';return '<div class="user"><div class="row"><div><div class="email">'+esc(u.full_name||u.email)+'</div><div class="meta">'+esc(u.email)+'</div><div class="meta">Contact: '+esc(u.contact_number||'—')+' • WhatsApp: '+esc(u.whatsapp_name||'—')+'</div><div class="meta">Address: '+esc(u.address||'—')+'</div><div class="meta">Created: '+new Date(u.created_at).toLocaleString()+'</div><div class="meta">Email: <span class="badge '+(verified?'verified':'unverified')+'">'+(verified?'VERIFIED':'NOT VERIFIED')+'</span></div></div><span class="badge '+(status==='APPROVED'?'approved':status==='REJECTED'?'rejected':'pending')+'">'+esc(status.replaceAll('_',' '))+'</span></div><div class="actions">'+(!verified&&status==='PENDING_EMAIL'?'<button class="action approve" onclick="verifyUser(\''+u.id+'\',this)">VERIFY EMAIL</button>':'')+(status!=='APPROVED'&&verified?'<button class="action approve" onclick="approveUser(\''+u.id+'\')">APPROVE</button>':'')+(status!=='REJECTED'?'<button class="action" onclick="rejectUser(\''+u.id+'\')">REJECT</button>':'')+(status==='APPROVED'||status==='REJECTED'?'<button class="action delete-account" onclick="deleteUser(\''+u.id+'\',this)">DELETE PERMANENTLY</button>':'')+'</div></div>'}).join('')}
window.verifyUser=async(id,btn)=>{if(!confirm('Manually verify this customer email? This bypasses the email OTP.'))return;if(btn){btn.disabled=true;btn.textContent='VERIFYING...'}const {data,error}=await sb.functions.invoke('admin-verify-customer-email',{body:{user_id:id}});if(error||data?.error){alert(error?.message||data?.error||'Verification failed.');if(btn){btn.disabled=false;btn.textContent='VERIFY EMAIL'}return}load()};
window.approveUser=async id=>{const {data:profile}=await sb.from('profiles').select('email_verified_at').eq('id',id).maybeSingle();if(!profile?.email_verified_at){alert('Verify the customer email first.');return}const {error}=await sb.from('profiles').update({account_status:'APPROVED',approved_at:new Date().toISOString(),rejected_at:null}).eq('id',id);if(error)alert(error.message);else load()};
window.rejectUser=async id=>{if(!confirm('Reject this account?'))return;const {error}=await sb.from('profiles').update({account_status:'REJECTED',rejected_at:new Date().toISOString(),approved_at:null}).eq('id',id);if(error)alert(error.message);else load()};
window.deleteUser=async(id,btn)=>{if(!confirm('PERMANENTLY DELETE this customer account? This will remove the login account and profile permanently. This cannot be undone.'))return;if(btn){btn.disabled=true;btn.textContent='DELETING...'}const {data,error}=await sb.functions.invoke('delete-customer-account',{body:{user_id:id}});if(error||data?.error){alert(error?.message||data?.error||'Delete failed.');if(btn){btn.disabled=false;btn.textContent='DELETE PERMANENTLY'}return}alert('Customer account permanently deleted.');load()};load()})();