(async()=>{let sb;let filter='PENDING';const list=document.getElementById('list');sb=initSupabase();try{await requireAdmin();}catch(e){list.innerHTML='<div class="empty">Admin access required. <a href="/admin.html">Go to admin login</a></div>';return}document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));b.classList.add('active');load()});async function load(){let q=sb.from('profiles').select('id,email,is_admin,account_status,email_verified_at,created_at,approved_at,rejected_at').eq('is_admin',false).order('created_at',{ascending:false});if(filter!=='ALL')q=q.eq('account_status',filter==='PENDING'?'EMAIL_VERIFIED_PENDING_ADMIN':filter);const {data,error}=await q;if(error){list.innerHTML='<div class="empty">'+error.message+'</div>';return}if(!data?.length){list.innerHTML='<div class="empty">No accounts in this section.</div>';return}list.innerHTML=data.map(u=>{const verified=!!u.email_verified_at;const status=u.account_status||'PENDING_EMAIL';return '<div class="user"><div class="row"><div><div class="email">'+esc(u.email)+'</div><div class="meta">Created: '+new Date(u.created_at).toLocaleString()+'</div><div class="meta">Email: <span class="badge '+(verified?'verified':'unverified')+'">'+(verified?'VERIFIED':'NOT VERIFIED')+'</span></div></div><span class="badge '+(status==='APPROVED'?'approved':status==='REJECTED'?'rejected':'pending')+'">'+esc(status.replaceAll('_',' '))+'</span></div><div class="actions">'+(status!=='APPROVED'?'<button class="action approve" onclick="approveUser(\''+u.id+'\')">APPROVE</button>':'')+(status!=='REJECTED'?'<button class="action" onclick="rejectUser(\''+u.id+'\')">REJECT</button>':'')+'</div></div>'}).join('')}window.approveUser=async id=>{const {error}=await sb.from('profiles').update({account_status:'APPROVED',approved_at:new Date().toISOString(),rejected_at:null}).eq('id',id);if(error)alert(error.message);else load()};window.rejectUser=async id=>{if(!confirm('Reject this account?'))return;const {error}=await sb.from('profiles').update({account_status:'REJECTED',rejected_at:new Date().toISOString(),approved_at:null}).eq('id',id);if(error)alert(error.message);else load()};load()})();